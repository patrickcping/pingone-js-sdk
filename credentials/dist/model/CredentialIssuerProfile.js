/**
 * PingOne Platform API - Credentials
 * The PingOne Platform API covering the PingOne Credentials service
 *
 * The version of the OpenAPI document: 2023-06-29
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import CredentialIssuerProfileApplicationInstance from './CredentialIssuerProfileApplicationInstance';
import LinksHATEOAS from './LinksHATEOAS';
import ObjectEnvironment from './ObjectEnvironment';

/**
 * The CredentialIssuerProfile model module.
 * @module model/CredentialIssuerProfile
 * @version 2023-06-29
 */
class CredentialIssuerProfile {
  /**
   * Constructs a new <code>CredentialIssuerProfile</code>.
   * @alias module:model/CredentialIssuerProfile
   * @param name {String} The name of the credential issuer. This will be included in credentials issued.
   */
  constructor(name) {
    CredentialIssuerProfile.initialize(this, name);
  }

  /**
   * Initializes the fields of this object.
   * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
   * Only for internal use.
   */
  static initialize(obj, name) {
    obj['name'] = name;
  }

  /**
   * Constructs a <code>CredentialIssuerProfile</code> from a plain JavaScript object, optionally creating a new instance.
   * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
   * @param {Object} data The plain JavaScript object bearing properties of interest.
   * @param {module:model/CredentialIssuerProfile} obj Optional instance to populate.
   * @return {module:model/CredentialIssuerProfile} The populated <code>CredentialIssuerProfile</code> instance.
   */
  static constructFromObject(data, obj) {
    if (data) {
      obj = obj || new CredentialIssuerProfile();
      if (data.hasOwnProperty('_links')) {
        obj['_links'] = LinksHATEOAS.constructFromObject(data['_links']);
      }
      if (data.hasOwnProperty('applicationInstance')) {
        obj['applicationInstance'] = CredentialIssuerProfileApplicationInstance.constructFromObject(data['applicationInstance']);
      }
      if (data.hasOwnProperty('createdAt')) {
        obj['createdAt'] = ApiClient.convertToType(data['createdAt'], 'Date');
      }
      if (data.hasOwnProperty('environment')) {
        obj['environment'] = ObjectEnvironment.constructFromObject(data['environment']);
      }
      if (data.hasOwnProperty('id')) {
        obj['id'] = ApiClient.convertToType(data['id'], 'String');
      }
      if (data.hasOwnProperty('name')) {
        obj['name'] = ApiClient.convertToType(data['name'], 'String');
      }
      if (data.hasOwnProperty('updatedAt')) {
        obj['updatedAt'] = ApiClient.convertToType(data['updatedAt'], 'Date');
      }
      if (data.hasOwnProperty('siteUrl')) {
        obj['siteUrl'] = ApiClient.convertToType(data['siteUrl'], 'String');
      }
      if (data.hasOwnProperty('customEmailTemplate')) {
        obj['customEmailTemplate'] = ApiClient.convertToType(data['customEmailTemplate'], 'String');
      }
    }
    return obj;
  }

  /**
   * Validates the JSON data with respect to <code>CredentialIssuerProfile</code>.
   * @param {Object} data The plain JavaScript object bearing properties of interest.
   * @return {boolean} to indicate whether the JSON data is valid with respect to <code>CredentialIssuerProfile</code>.
   */
  static validateJSON(data) {
    // check to make sure all required properties are present in the JSON string
    for (const property of CredentialIssuerProfile.RequiredProperties) {
      if (!data.hasOwnProperty(property)) {
        throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
      }
    }
    // validate the optional field `applicationInstance`
    if (data['applicationInstance']) {
      // data not null
      CredentialIssuerProfileApplicationInstance.validateJSON(data['applicationInstance']);
    }
    // validate the optional field `environment`
    if (data['environment']) {
      // data not null
      ObjectEnvironment.validateJSON(data['environment']);
    }
    // ensure the json data is a string
    if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
      throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
    }
    // ensure the json data is a string
    if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
      throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
    }
    // ensure the json data is a string
    if (data['siteUrl'] && !(typeof data['siteUrl'] === 'string' || data['siteUrl'] instanceof String)) {
      throw new Error("Expected the field `siteUrl` to be a primitive type in the JSON string but got " + data['siteUrl']);
    }
    // ensure the json data is a string
    if (data['customEmailTemplate'] && !(typeof data['customEmailTemplate'] === 'string' || data['customEmailTemplate'] instanceof String)) {
      throw new Error("Expected the field `customEmailTemplate` to be a primitive type in the JSON string but got " + data['customEmailTemplate']);
    }
    return true;
  }
}
CredentialIssuerProfile.RequiredProperties = ["name"];

/**
 * @member {module:model/LinksHATEOAS} _links
 */
CredentialIssuerProfile.prototype['_links'] = undefined;

/**
 * @member {module:model/CredentialIssuerProfileApplicationInstance} applicationInstance
 */
CredentialIssuerProfile.prototype['applicationInstance'] = undefined;

/**
 * A string that specifies the date and time the issuer profile was created.
 * @member {Date} createdAt
 */
CredentialIssuerProfile.prototype['createdAt'] = undefined;

/**
 * @member {module:model/ObjectEnvironment} environment
 */
CredentialIssuerProfile.prototype['environment'] = undefined;

/**
 * A string that specifies the identifier (UUID) of the credential issuer.
 * @member {String} id
 */
CredentialIssuerProfile.prototype['id'] = undefined;

/**
 * The name of the credential issuer. This will be included in credentials issued.
 * @member {String} name
 */
CredentialIssuerProfile.prototype['name'] = undefined;

/**
 * A string that specifies the date and time the credential issuer profile was last updated; can be null.
 * @member {Date} updatedAt
 */
CredentialIssuerProfile.prototype['updatedAt'] = undefined;

/**
 * A string that specifies the base URL associated with the credential issuer.
 * @member {String} siteUrl
 */
CredentialIssuerProfile.prototype['siteUrl'] = undefined;

/**
 * A string that specifies the default notification template used in credential issuance notifications. Deprecated.
 * @member {String} customEmailTemplate
 */
CredentialIssuerProfile.prototype['customEmailTemplate'] = undefined;
export default CredentialIssuerProfile;