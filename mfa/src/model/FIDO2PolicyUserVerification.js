/**
 * PingOne Platform API - PingOne MFA
 * The PingOne Platform API covering the PingOne MFA service
 *
 * The version of the OpenAPI document: 2023-06-29
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import EnumFIDO2PolicyUserVerificationOption from './EnumFIDO2PolicyUserVerificationOption';

/**
 * The FIDO2PolicyUserVerification model module.
 * @module model/FIDO2PolicyUserVerification
 * @version 2023-06-29
 */
class FIDO2PolicyUserVerification {
    /**
     * Constructs a new <code>FIDO2PolicyUserVerification</code>.
     * Used to control whether the user must perform a gesture (such as a public key credential, fingerprint scan, or a PIN code) when registering or authenticating with their FIDO device.
     * @alias module:model/FIDO2PolicyUserVerification
     * @param enforceDuringAuthentication {Boolean} Set to `true` if you want the device characteristics related to user verification to be checked again at each authentication attempt and not just once during registration. Set to `false` to have them checked only at registration.
     * @param option {module:model/EnumFIDO2PolicyUserVerificationOption} 
     */
    constructor(enforceDuringAuthentication, option) { 
        
        FIDO2PolicyUserVerification.initialize(this, enforceDuringAuthentication, option);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, enforceDuringAuthentication, option) { 
        obj['enforceDuringAuthentication'] = enforceDuringAuthentication;
        obj['option'] = option;
    }

    /**
     * Constructs a <code>FIDO2PolicyUserVerification</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/FIDO2PolicyUserVerification} obj Optional instance to populate.
     * @return {module:model/FIDO2PolicyUserVerification} The populated <code>FIDO2PolicyUserVerification</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new FIDO2PolicyUserVerification();

            if (data.hasOwnProperty('enforceDuringAuthentication')) {
                obj['enforceDuringAuthentication'] = ApiClient.convertToType(data['enforceDuringAuthentication'], 'Boolean');
            }
            if (data.hasOwnProperty('option')) {
                obj['option'] = EnumFIDO2PolicyUserVerificationOption.constructFromObject(data['option']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>FIDO2PolicyUserVerification</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>FIDO2PolicyUserVerification</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of FIDO2PolicyUserVerification.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }

        return true;
    }


}

FIDO2PolicyUserVerification.RequiredProperties = ["enforceDuringAuthentication", "option"];

/**
 * Set to `true` if you want the device characteristics related to user verification to be checked again at each authentication attempt and not just once during registration. Set to `false` to have them checked only at registration.
 * @member {Boolean} enforceDuringAuthentication
 */
FIDO2PolicyUserVerification.prototype['enforceDuringAuthentication'] = undefined;

/**
 * @member {module:model/EnumFIDO2PolicyUserVerificationOption} option
 */
FIDO2PolicyUserVerification.prototype['option'] = undefined;






export default FIDO2PolicyUserVerification;

