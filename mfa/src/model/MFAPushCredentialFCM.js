/**
 * PingOne Platform API - PingOne MFA
 * The PingOne Platform API covering the PingOne MFA service
 *
 * The version of the OpenAPI document: 2023-06-29
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import EnumMFAPushCredentialAttrType from './EnumMFAPushCredentialAttrType';
import MFAPushCredential from './MFAPushCredential';

/**
 * The MFAPushCredentialFCM model module.
 * @module model/MFAPushCredentialFCM
 * @version 2023-06-29
 */
class MFAPushCredentialFCM {
    /**
     * Constructs a new <code>MFAPushCredentialFCM</code>.
     * @alias module:model/MFAPushCredentialFCM
     * @implements module:model/MFAPushCredential
     * @param type {module:model/EnumMFAPushCredentialAttrType} 
     * @param key {String} A string that Apple uses as an identifier to identify an authentication key.  Mandatory.
     */
    constructor(type, key) { 
        MFAPushCredential.initialize(this, type);
        MFAPushCredentialFCM.initialize(this, type, key);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, type, key) { 
        obj['type'] = type;
        obj['key'] = key;
    }

    /**
     * Constructs a <code>MFAPushCredentialFCM</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/MFAPushCredentialFCM} obj Optional instance to populate.
     * @return {module:model/MFAPushCredentialFCM} The populated <code>MFAPushCredentialFCM</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new MFAPushCredentialFCM();
            MFAPushCredential.constructFromObject(data, obj);

            if (data.hasOwnProperty('type')) {
                obj['type'] = EnumMFAPushCredentialAttrType.constructFromObject(data['type']);
            }
            if (data.hasOwnProperty('key')) {
                obj['key'] = ApiClient.convertToType(data['key'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>MFAPushCredentialFCM</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>MFAPushCredentialFCM</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of MFAPushCredentialFCM.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['key'] && !(typeof data['key'] === 'string' || data['key'] instanceof String)) {
            throw new Error("Expected the field `key` to be a primitive type in the JSON string but got " + data['key']);
        }

        return true;
    }


}

MFAPushCredentialFCM.RequiredProperties = ["type", "key"];

/**
 * @member {module:model/EnumMFAPushCredentialAttrType} type
 */
MFAPushCredentialFCM.prototype['type'] = undefined;

/**
 * A string that Apple uses as an identifier to identify an authentication key.  Mandatory.
 * @member {String} key
 */
MFAPushCredentialFCM.prototype['key'] = undefined;


// Implement MFAPushCredential interface:
/**
 * @member {module:model/EnumMFAPushCredentialAttrType} type
 */
MFAPushCredential.prototype['type'] = undefined;




export default MFAPushCredentialFCM;

