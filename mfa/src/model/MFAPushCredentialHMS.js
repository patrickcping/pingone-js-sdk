/**
 * PingOne Platform API - PingOne MFA
 * The PingOne Platform API covering the PingOne MFA service
 *
 * The version of the OpenAPI document: 2023-06-29
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import EnumMFAPushCredentialAttrType from './EnumMFAPushCredentialAttrType';
import MFAPushCredential from './MFAPushCredential';

/**
 * The MFAPushCredentialHMS model module.
 * @module model/MFAPushCredentialHMS
 * @version 2023-06-29
 */
class MFAPushCredentialHMS {
    /**
     * Constructs a new <code>MFAPushCredentialHMS</code>.
     * @alias module:model/MFAPushCredentialHMS
     * @implements module:model/MFAPushCredential
     * @param type {module:model/EnumMFAPushCredentialAttrType} 
     * @param clientId {String} Used only if type is set to HMS. OAuth 2.0 Client ID from the Huawei Developers API console.
     * @param clientSecret {String} Used only if type is set to HMS. The client secret associated with the OAuth 2.0 Client ID.
     */
    constructor(type, clientId, clientSecret) { 
        MFAPushCredential.initialize(this, type);
        MFAPushCredentialHMS.initialize(this, type, clientId, clientSecret);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, type, clientId, clientSecret) { 
        obj['type'] = type;
        obj['clientId'] = clientId;
        obj['clientSecret'] = clientSecret;
    }

    /**
     * Constructs a <code>MFAPushCredentialHMS</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/MFAPushCredentialHMS} obj Optional instance to populate.
     * @return {module:model/MFAPushCredentialHMS} The populated <code>MFAPushCredentialHMS</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new MFAPushCredentialHMS();
            MFAPushCredential.constructFromObject(data, obj);

            if (data.hasOwnProperty('type')) {
                obj['type'] = EnumMFAPushCredentialAttrType.constructFromObject(data['type']);
            }
            if (data.hasOwnProperty('clientId')) {
                obj['clientId'] = ApiClient.convertToType(data['clientId'], 'String');
            }
            if (data.hasOwnProperty('clientSecret')) {
                obj['clientSecret'] = ApiClient.convertToType(data['clientSecret'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>MFAPushCredentialHMS</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>MFAPushCredentialHMS</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of MFAPushCredentialHMS.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['clientId'] && !(typeof data['clientId'] === 'string' || data['clientId'] instanceof String)) {
            throw new Error("Expected the field `clientId` to be a primitive type in the JSON string but got " + data['clientId']);
        }
        // ensure the json data is a string
        if (data['clientSecret'] && !(typeof data['clientSecret'] === 'string' || data['clientSecret'] instanceof String)) {
            throw new Error("Expected the field `clientSecret` to be a primitive type in the JSON string but got " + data['clientSecret']);
        }

        return true;
    }


}

MFAPushCredentialHMS.RequiredProperties = ["type", "clientId", "clientSecret"];

/**
 * @member {module:model/EnumMFAPushCredentialAttrType} type
 */
MFAPushCredentialHMS.prototype['type'] = undefined;

/**
 * Used only if type is set to HMS. OAuth 2.0 Client ID from the Huawei Developers API console.
 * @member {String} clientId
 */
MFAPushCredentialHMS.prototype['clientId'] = undefined;

/**
 * Used only if type is set to HMS. The client secret associated with the OAuth 2.0 Client ID.
 * @member {String} clientSecret
 */
MFAPushCredentialHMS.prototype['clientSecret'] = undefined;


// Implement MFAPushCredential interface:
/**
 * @member {module:model/EnumMFAPushCredentialAttrType} type
 */
MFAPushCredential.prototype['type'] = undefined;




export default MFAPushCredentialHMS;

