/**
 * PingOne Platform API - PingOne Verify
 * The PingOne Platform API covering the PingOne Verify service
 *
 * The version of the OpenAPI document: 2023-07-20
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import VerifyPolicy from './VerifyPolicy';
import VoicePhrase from './VoicePhrase';
import VoicePhraseContents from './VoicePhraseContents';

/**
 * The EntityArrayEmbedded model module.
 * @module model/EntityArrayEmbedded
 * @version 2023-07-20
 */
class EntityArrayEmbedded {
  /**
   * Constructs a new <code>EntityArrayEmbedded</code>.
   * @alias module:model/EntityArrayEmbedded
   */
  constructor() {
    EntityArrayEmbedded.initialize(this);
  }

  /**
   * Initializes the fields of this object.
   * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
   * Only for internal use.
   */
  static initialize(obj) {}

  /**
   * Constructs a <code>EntityArrayEmbedded</code> from a plain JavaScript object, optionally creating a new instance.
   * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
   * @param {Object} data The plain JavaScript object bearing properties of interest.
   * @param {module:model/EntityArrayEmbedded} obj Optional instance to populate.
   * @return {module:model/EntityArrayEmbedded} The populated <code>EntityArrayEmbedded</code> instance.
   */
  static constructFromObject(data, obj) {
    if (data) {
      obj = obj || new EntityArrayEmbedded();
      if (data.hasOwnProperty('verifyPolicies')) {
        obj['verifyPolicies'] = ApiClient.convertToType(data['verifyPolicies'], [VerifyPolicy]);
      }
      if (data.hasOwnProperty('voicePhrases')) {
        obj['voicePhrases'] = ApiClient.convertToType(data['voicePhrases'], [VoicePhrase]);
      }
      if (data.hasOwnProperty('contents')) {
        obj['contents'] = ApiClient.convertToType(data['contents'], [VoicePhraseContents]);
      }
    }
    return obj;
  }

  /**
   * Validates the JSON data with respect to <code>EntityArrayEmbedded</code>.
   * @param {Object} data The plain JavaScript object bearing properties of interest.
   * @return {boolean} to indicate whether the JSON data is valid with respect to <code>EntityArrayEmbedded</code>.
   */
  static validateJSON(data) {
    if (data['verifyPolicies']) {
      // data not null
      // ensure the json data is an array
      if (!Array.isArray(data['verifyPolicies'])) {
        throw new Error("Expected the field `verifyPolicies` to be an array in the JSON data but got " + data['verifyPolicies']);
      }
      // validate the optional field `verifyPolicies` (array)
      for (const item of data['verifyPolicies']) {
        VerifyPolicy.validateJSON(item);
      }
      ;
    }
    if (data['voicePhrases']) {
      // data not null
      // ensure the json data is an array
      if (!Array.isArray(data['voicePhrases'])) {
        throw new Error("Expected the field `voicePhrases` to be an array in the JSON data but got " + data['voicePhrases']);
      }
      // validate the optional field `voicePhrases` (array)
      for (const item of data['voicePhrases']) {
        VoicePhrase.validateJSON(item);
      }
      ;
    }
    if (data['contents']) {
      // data not null
      // ensure the json data is an array
      if (!Array.isArray(data['contents'])) {
        throw new Error("Expected the field `contents` to be an array in the JSON data but got " + data['contents']);
      }
      // validate the optional field `contents` (array)
      for (const item of data['contents']) {
        VoicePhraseContents.validateJSON(item);
      }
      ;
    }
    return true;
  }
}

/**
 * @member {Array.<module:model/VerifyPolicy>} verifyPolicies
 */
EntityArrayEmbedded.prototype['verifyPolicies'] = undefined;

/**
 * @member {Array.<module:model/VoicePhrase>} voicePhrases
 */
EntityArrayEmbedded.prototype['voicePhrases'] = undefined;

/**
 * @member {Array.<module:model/VoicePhraseContents>} contents
 */
EntityArrayEmbedded.prototype['contents'] = undefined;
export default EntityArrayEmbedded;