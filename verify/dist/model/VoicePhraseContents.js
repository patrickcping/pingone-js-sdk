/**
 * PingOne Platform API - PingOne Verify
 * The PingOne Platform API covering the PingOne Verify service
 *
 * The version of the OpenAPI document: 2023-07-20
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import ObjectEnvironment from './ObjectEnvironment';
import VoicePhraseContentsVoicePhrase from './VoicePhraseContentsVoicePhrase';

/**
 * The VoicePhraseContents model module.
 * @module model/VoicePhraseContents
 * @version 2023-07-20
 */
class VoicePhraseContents {
  /**
   * Constructs a new <code>VoicePhraseContents</code>.
   * @alias module:model/VoicePhraseContents
   * @param voicePhrase {module:model/VoicePhraseContentsVoicePhrase} 
   * @param locale {String} 
   * @param content {String} 
   */
  constructor(voicePhrase, locale, content) {
    VoicePhraseContents.initialize(this, voicePhrase, locale, content);
  }

  /**
   * Initializes the fields of this object.
   * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
   * Only for internal use.
   */
  static initialize(obj, voicePhrase, locale, content) {
    obj['voicePhrase'] = voicePhrase;
    obj['locale'] = locale;
    obj['content'] = content;
  }

  /**
   * Constructs a <code>VoicePhraseContents</code> from a plain JavaScript object, optionally creating a new instance.
   * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
   * @param {Object} data The plain JavaScript object bearing properties of interest.
   * @param {module:model/VoicePhraseContents} obj Optional instance to populate.
   * @return {module:model/VoicePhraseContents} The populated <code>VoicePhraseContents</code> instance.
   */
  static constructFromObject(data, obj) {
    if (data) {
      obj = obj || new VoicePhraseContents();
      if (data.hasOwnProperty('id')) {
        obj['id'] = ApiClient.convertToType(data['id'], 'String');
      }
      if (data.hasOwnProperty('environment')) {
        obj['environment'] = ObjectEnvironment.constructFromObject(data['environment']);
      }
      if (data.hasOwnProperty('voicePhrase')) {
        obj['voicePhrase'] = VoicePhraseContentsVoicePhrase.constructFromObject(data['voicePhrase']);
      }
      if (data.hasOwnProperty('locale')) {
        obj['locale'] = ApiClient.convertToType(data['locale'], 'String');
      }
      if (data.hasOwnProperty('content')) {
        obj['content'] = ApiClient.convertToType(data['content'], 'String');
      }
      if (data.hasOwnProperty('createdAt')) {
        obj['createdAt'] = ApiClient.convertToType(data['createdAt'], 'Date');
      }
      if (data.hasOwnProperty('updatedAt')) {
        obj['updatedAt'] = ApiClient.convertToType(data['updatedAt'], 'Date');
      }
    }
    return obj;
  }

  /**
   * Validates the JSON data with respect to <code>VoicePhraseContents</code>.
   * @param {Object} data The plain JavaScript object bearing properties of interest.
   * @return {boolean} to indicate whether the JSON data is valid with respect to <code>VoicePhraseContents</code>.
   */
  static validateJSON(data) {
    // check to make sure all required properties are present in the JSON string
    for (const property of VoicePhraseContents.RequiredProperties) {
      if (!data.hasOwnProperty(property)) {
        throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
      }
    }
    // ensure the json data is a string
    if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
      throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
    }
    // validate the optional field `environment`
    if (data['environment']) {
      // data not null
      ObjectEnvironment.validateJSON(data['environment']);
    }
    // validate the optional field `voicePhrase`
    if (data['voicePhrase']) {
      // data not null
      VoicePhraseContentsVoicePhrase.validateJSON(data['voicePhrase']);
    }
    // ensure the json data is a string
    if (data['locale'] && !(typeof data['locale'] === 'string' || data['locale'] instanceof String)) {
      throw new Error("Expected the field `locale` to be a primitive type in the JSON string but got " + data['locale']);
    }
    // ensure the json data is a string
    if (data['content'] && !(typeof data['content'] === 'string' || data['content'] instanceof String)) {
      throw new Error("Expected the field `content` to be a primitive type in the JSON string but got " + data['content']);
    }
    return true;
  }
}
VoicePhraseContents.RequiredProperties = ["voicePhrase", "locale", "content"];

/**
 * @member {String} id
 */
VoicePhraseContents.prototype['id'] = undefined;

/**
 * @member {module:model/ObjectEnvironment} environment
 */
VoicePhraseContents.prototype['environment'] = undefined;

/**
 * @member {module:model/VoicePhraseContentsVoicePhrase} voicePhrase
 */
VoicePhraseContents.prototype['voicePhrase'] = undefined;

/**
 * @member {String} locale
 */
VoicePhraseContents.prototype['locale'] = undefined;

/**
 * @member {String} content
 */
VoicePhraseContents.prototype['content'] = undefined;

/**
 * @member {Date} createdAt
 */
VoicePhraseContents.prototype['createdAt'] = undefined;

/**
 * @member {Date} updatedAt
 */
VoicePhraseContents.prototype['updatedAt'] = undefined;
export default VoicePhraseContents;