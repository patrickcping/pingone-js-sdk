/**
 * PingOne Platform API - PingOne Verify
 * The PingOne Platform API covering the PingOne Verify service
 *
 * The version of the OpenAPI document: 2023-07-20
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import EnumVerify from './EnumVerify';
import VoiceConfigurationReferenceData from './VoiceConfigurationReferenceData';
import VoiceConfigurationTextDependent from './VoiceConfigurationTextDependent';
import VoiceConfigurationThreshold from './VoiceConfigurationThreshold';

/**
 * The VoiceConfiguration model module.
 * @module model/VoiceConfiguration
 * @version 2023-07-20
 */
class VoiceConfiguration {
    /**
     * Constructs a new <code>VoiceConfiguration</code>.
     * @alias module:model/VoiceConfiguration
     * @param verify {module:model/EnumVerify} 
     * @param enrollment {Boolean} 
     * @param comparison {module:model/VoiceConfigurationThreshold} 
     * @param liveness {module:model/VoiceConfigurationThreshold} 
     */
    constructor(verify, enrollment, comparison, liveness) { 
        
        VoiceConfiguration.initialize(this, verify, enrollment, comparison, liveness);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, verify, enrollment, comparison, liveness) { 
        obj['verify'] = verify;
        obj['enrollment'] = enrollment;
        obj['comparison'] = comparison;
        obj['liveness'] = liveness;
    }

    /**
     * Constructs a <code>VoiceConfiguration</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/VoiceConfiguration} obj Optional instance to populate.
     * @return {module:model/VoiceConfiguration} The populated <code>VoiceConfiguration</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new VoiceConfiguration();

            if (data.hasOwnProperty('verify')) {
                obj['verify'] = EnumVerify.constructFromObject(data['verify']);
            }
            if (data.hasOwnProperty('enrollment')) {
                obj['enrollment'] = ApiClient.convertToType(data['enrollment'], 'Boolean');
            }
            if (data.hasOwnProperty('textDependent')) {
                obj['textDependent'] = VoiceConfigurationTextDependent.constructFromObject(data['textDependent']);
            }
            if (data.hasOwnProperty('comparison')) {
                obj['comparison'] = VoiceConfigurationThreshold.constructFromObject(data['comparison']);
            }
            if (data.hasOwnProperty('liveness')) {
                obj['liveness'] = VoiceConfigurationThreshold.constructFromObject(data['liveness']);
            }
            if (data.hasOwnProperty('referenceData')) {
                obj['referenceData'] = VoiceConfigurationReferenceData.constructFromObject(data['referenceData']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>VoiceConfiguration</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>VoiceConfiguration</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of VoiceConfiguration.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // validate the optional field `textDependent`
        if (data['textDependent']) { // data not null
          VoiceConfigurationTextDependent.validateJSON(data['textDependent']);
        }
        // validate the optional field `comparison`
        if (data['comparison']) { // data not null
          VoiceConfigurationThreshold.validateJSON(data['comparison']);
        }
        // validate the optional field `liveness`
        if (data['liveness']) { // data not null
          VoiceConfigurationThreshold.validateJSON(data['liveness']);
        }
        // validate the optional field `referenceData`
        if (data['referenceData']) { // data not null
          VoiceConfigurationReferenceData.validateJSON(data['referenceData']);
        }

        return true;
    }


}

VoiceConfiguration.RequiredProperties = ["verify", "enrollment", "comparison", "liveness"];

/**
 * @member {module:model/EnumVerify} verify
 */
VoiceConfiguration.prototype['verify'] = undefined;

/**
 * @member {Boolean} enrollment
 */
VoiceConfiguration.prototype['enrollment'] = undefined;

/**
 * @member {module:model/VoiceConfigurationTextDependent} textDependent
 */
VoiceConfiguration.prototype['textDependent'] = undefined;

/**
 * @member {module:model/VoiceConfigurationThreshold} comparison
 */
VoiceConfiguration.prototype['comparison'] = undefined;

/**
 * @member {module:model/VoiceConfigurationThreshold} liveness
 */
VoiceConfiguration.prototype['liveness'] = undefined;

/**
 * @member {module:model/VoiceConfigurationReferenceData} referenceData
 */
VoiceConfiguration.prototype['referenceData'] = undefined;






export default VoiceConfiguration;

