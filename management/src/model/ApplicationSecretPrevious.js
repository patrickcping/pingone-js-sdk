/**
 * PingOne Platform API - SSO and Base
 * The PingOne Platform API covering the base and SSO services (otherwise known as the Management APIs)
 *
 * The version of the OpenAPI document: 2023-06-29
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The ApplicationSecretPrevious model module.
 * @module model/ApplicationSecretPrevious
 * @version 2023-06-29
 */
class ApplicationSecretPrevious {
    /**
     * Constructs a new <code>ApplicationSecretPrevious</code>.
     * @alias module:model/ApplicationSecretPrevious
     * @param expiresAt {Date} A timestamp that specifies how long this secret is saved (and can be used) before it expires. Supported time range is 1 minute to 30 days.
     */
    constructor(expiresAt) { 
        
        ApplicationSecretPrevious.initialize(this, expiresAt);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, expiresAt) { 
        obj['expiresAt'] = expiresAt;
    }

    /**
     * Constructs a <code>ApplicationSecretPrevious</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ApplicationSecretPrevious} obj Optional instance to populate.
     * @return {module:model/ApplicationSecretPrevious} The populated <code>ApplicationSecretPrevious</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ApplicationSecretPrevious();

            if (data.hasOwnProperty('secret')) {
                obj['secret'] = ApiClient.convertToType(data['secret'], 'String');
            }
            if (data.hasOwnProperty('expiresAt')) {
                obj['expiresAt'] = ApiClient.convertToType(data['expiresAt'], 'Date');
            }
            if (data.hasOwnProperty('lastUsed')) {
                obj['lastUsed'] = ApiClient.convertToType(data['lastUsed'], 'Date');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ApplicationSecretPrevious</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ApplicationSecretPrevious</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of ApplicationSecretPrevious.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['secret'] && !(typeof data['secret'] === 'string' || data['secret'] instanceof String)) {
            throw new Error("Expected the field `secret` to be a primitive type in the JSON string but got " + data['secret']);
        }

        return true;
    }


}

ApplicationSecretPrevious.RequiredProperties = ["expiresAt"];

/**
 * A string that specifies the previous application secret. This property is returned in the response if the previous secret is not expired.
 * @member {String} secret
 */
ApplicationSecretPrevious.prototype['secret'] = undefined;

/**
 * A timestamp that specifies how long this secret is saved (and can be used) before it expires. Supported time range is 1 minute to 30 days.
 * @member {Date} expiresAt
 */
ApplicationSecretPrevious.prototype['expiresAt'] = undefined;

/**
 * A timestamp that specifies when the previous secret was last used.
 * @member {Date} lastUsed
 */
ApplicationSecretPrevious.prototype['lastUsed'] = undefined;






export default ApplicationSecretPrevious;

