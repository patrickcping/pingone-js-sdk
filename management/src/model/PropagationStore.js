/**
 * PingOne Platform API - SSO and Base
 * The PingOne Platform API covering the base and SSO services (otherwise known as the Management APIs)
 *
 * The version of the OpenAPI document: 2023-06-29
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import EnumPropagationStoreStatus from './EnumPropagationStoreStatus';
import EnumPropagationStoreType from './EnumPropagationStoreType';
import LinksHATEOAS from './LinksHATEOAS';
import ObjectEnvironment from './ObjectEnvironment';
import PropagationStoreImage from './PropagationStoreImage';
import PropagationStoreSyncStatus from './PropagationStoreSyncStatus';

/**
 * The PropagationStore model module.
 * @module model/PropagationStore
 * @version 2023-06-29
 */
class PropagationStore {
    /**
     * Constructs a new <code>PropagationStore</code>.
     * @alias module:model/PropagationStore
     * @param configuration {Object.<String, Object>} Configuration properties specific to each identity propagation store.
     * @param name {String} The name of the identity store.
     * @param type {module:model/EnumPropagationStoreType} 
     */
    constructor(configuration, name, type) { 
        
        PropagationStore.initialize(this, configuration, name, type);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, configuration, name, type) { 
        obj['configuration'] = configuration;
        obj['name'] = name;
        obj['type'] = type;
    }

    /**
     * Constructs a <code>PropagationStore</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/PropagationStore} obj Optional instance to populate.
     * @return {module:model/PropagationStore} The populated <code>PropagationStore</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new PropagationStore();

            if (data.hasOwnProperty('_links')) {
                obj['_links'] = LinksHATEOAS.constructFromObject(data['_links']);
            }
            if (data.hasOwnProperty('configuration')) {
                obj['configuration'] = ApiClient.convertToType(data['configuration'], {'String': Object});
            }
            if (data.hasOwnProperty('description')) {
                obj['description'] = ApiClient.convertToType(data['description'], 'String');
            }
            if (data.hasOwnProperty('environment')) {
                obj['environment'] = ObjectEnvironment.constructFromObject(data['environment']);
            }
            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('image')) {
                obj['image'] = PropagationStoreImage.constructFromObject(data['image']);
            }
            if (data.hasOwnProperty('managed')) {
                obj['managed'] = ApiClient.convertToType(data['managed'], 'Boolean');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('status')) {
                obj['status'] = EnumPropagationStoreStatus.constructFromObject(data['status']);
            }
            if (data.hasOwnProperty('syncStatus')) {
                obj['syncStatus'] = PropagationStoreSyncStatus.constructFromObject(data['syncStatus']);
            }
            if (data.hasOwnProperty('type')) {
                obj['type'] = EnumPropagationStoreType.constructFromObject(data['type']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>PropagationStore</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>PropagationStore</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of PropagationStore.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['description'] && !(typeof data['description'] === 'string' || data['description'] instanceof String)) {
            throw new Error("Expected the field `description` to be a primitive type in the JSON string but got " + data['description']);
        }
        // validate the optional field `environment`
        if (data['environment']) { // data not null
          ObjectEnvironment.validateJSON(data['environment']);
        }
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // validate the optional field `image`
        if (data['image']) { // data not null
          PropagationStoreImage.validateJSON(data['image']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // validate the optional field `syncStatus`
        if (data['syncStatus']) { // data not null
          PropagationStoreSyncStatus.validateJSON(data['syncStatus']);
        }

        return true;
    }


}

PropagationStore.RequiredProperties = ["configuration", "name", "type"];

/**
 * @member {module:model/LinksHATEOAS} _links
 */
PropagationStore.prototype['_links'] = undefined;

/**
 * Configuration properties specific to each identity propagation store.
 * @member {Object.<String, Object>} configuration
 */
PropagationStore.prototype['configuration'] = undefined;

/**
 * A description for the identity propagation store.
 * @member {String} description
 */
PropagationStore.prototype['description'] = undefined;

/**
 * @member {module:model/ObjectEnvironment} environment
 */
PropagationStore.prototype['environment'] = undefined;

/**
 * A string that specifies the resource’s unique identifier.
 * @member {String} id
 */
PropagationStore.prototype['id'] = undefined;

/**
 * @member {module:model/PropagationStoreImage} image
 */
PropagationStore.prototype['image'] = undefined;

/**
 * Indicates whether or not to enable deprovisioning of users for a store when it is deleted. The deprovisioning occurs when a new revision is created (`POST {{apiPath}}/environments/{{envID}}/propagation/revisions`).
 * @member {Boolean} managed
 */
PropagationStore.prototype['managed'] = undefined;

/**
 * The name of the identity store.
 * @member {String} name
 */
PropagationStore.prototype['name'] = undefined;

/**
 * @member {module:model/EnumPropagationStoreStatus} status
 */
PropagationStore.prototype['status'] = undefined;

/**
 * @member {module:model/PropagationStoreSyncStatus} syncStatus
 */
PropagationStore.prototype['syncStatus'] = undefined;

/**
 * @member {module:model/EnumPropagationStoreType} type
 */
PropagationStore.prototype['type'] = undefined;






export default PropagationStore;

