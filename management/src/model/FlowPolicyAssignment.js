/**
 * PingOne Platform API - SSO and Base
 * The PingOne Platform API covering the base and SSO services (otherwise known as the Management APIs)
 *
 * The version of the OpenAPI document: 2023-06-29
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import FlowPolicyAssignmentFlowPolicy from './FlowPolicyAssignmentFlowPolicy';
import LinksHATEOAS from './LinksHATEOAS';
import ObjectApplication from './ObjectApplication';
import ObjectEnvironment from './ObjectEnvironment';

/**
 * The FlowPolicyAssignment model module.
 * @module model/FlowPolicyAssignment
 * @version 2023-06-29
 */
class FlowPolicyAssignment {
    /**
     * Constructs a new <code>FlowPolicyAssignment</code>.
     * @alias module:model/FlowPolicyAssignment
     * @param flowPolicy {module:model/FlowPolicyAssignmentFlowPolicy} 
     * @param priority {Number} The order in which the policy referenced by this assignment is evaluated during an authentication flow relative to other policies. An assignment with a lower priority will be evaluated first.
     */
    constructor(flowPolicy, priority) { 
        
        FlowPolicyAssignment.initialize(this, flowPolicy, priority);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, flowPolicy, priority) { 
        obj['flowPolicy'] = flowPolicy;
        obj['priority'] = priority;
    }

    /**
     * Constructs a <code>FlowPolicyAssignment</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/FlowPolicyAssignment} obj Optional instance to populate.
     * @return {module:model/FlowPolicyAssignment} The populated <code>FlowPolicyAssignment</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new FlowPolicyAssignment();

            if (data.hasOwnProperty('_links')) {
                obj['_links'] = LinksHATEOAS.constructFromObject(data['_links']);
            }
            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('application')) {
                obj['application'] = ObjectApplication.constructFromObject(data['application']);
            }
            if (data.hasOwnProperty('environment')) {
                obj['environment'] = ObjectEnvironment.constructFromObject(data['environment']);
            }
            if (data.hasOwnProperty('flowPolicy')) {
                obj['flowPolicy'] = FlowPolicyAssignmentFlowPolicy.constructFromObject(data['flowPolicy']);
            }
            if (data.hasOwnProperty('priority')) {
                obj['priority'] = ApiClient.convertToType(data['priority'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>FlowPolicyAssignment</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>FlowPolicyAssignment</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of FlowPolicyAssignment.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // validate the optional field `application`
        if (data['application']) { // data not null
          ObjectApplication.validateJSON(data['application']);
        }
        // validate the optional field `environment`
        if (data['environment']) { // data not null
          ObjectEnvironment.validateJSON(data['environment']);
        }
        // validate the optional field `flowPolicy`
        if (data['flowPolicy']) { // data not null
          FlowPolicyAssignmentFlowPolicy.validateJSON(data['flowPolicy']);
        }

        return true;
    }


}

FlowPolicyAssignment.RequiredProperties = ["flowPolicy", "priority"];

/**
 * @member {module:model/LinksHATEOAS} _links
 */
FlowPolicyAssignment.prototype['_links'] = undefined;

/**
 * A string that specifies the flow policy assignment resource's unique identifier.
 * @member {String} id
 */
FlowPolicyAssignment.prototype['id'] = undefined;

/**
 * @member {module:model/ObjectApplication} application
 */
FlowPolicyAssignment.prototype['application'] = undefined;

/**
 * @member {module:model/ObjectEnvironment} environment
 */
FlowPolicyAssignment.prototype['environment'] = undefined;

/**
 * @member {module:model/FlowPolicyAssignmentFlowPolicy} flowPolicy
 */
FlowPolicyAssignment.prototype['flowPolicy'] = undefined;

/**
 * The order in which the policy referenced by this assignment is evaluated during an authentication flow relative to other policies. An assignment with a lower priority will be evaluated first.
 * @member {Number} priority
 */
FlowPolicyAssignment.prototype['priority'] = undefined;






export default FlowPolicyAssignment;

